{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block users %}
    {{parent()}}
    <style type="text/css">
        .block_users { background-color: #c7c7c7}
    </style>
{% endblock %}

{% block content %}
   <div class="container">
        <h4 class="text-center py-2">Détails utilisateur</h4>

        <table class="table table-hover table-stripped">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ user.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Username</th>
                    <td>{{ user.username }}</td>
                </tr>
                {# <tr>
                    <th>Password</th>
                    <td>{{ user.password }}</td>
                </tr> #}
                <tr>
                    <th>Roles</th>
                    <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
                </tr>
                <tr>
                    <th>Status</th>
                    <td>{{ user.status }}</td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex d-inline justify-content-center">

            <a href="{{ path('user_index') }}" class="d-flex pb-2 mr-2 ">
                <i class="fa fa-arrow-left" aria-hidden="true"></i>
                <h6 class="ml-2 text-dark ">Back</h6>
            </a>
            <a href="{{ path('user_edit', {'id': user.id}) }}" class="pb-2 mr-2"><button class="btn btn-action">Modifier</button></a>

            {{ include('user/_delete_form.html.twig') }}

        </div>
        
    </div>
{% endblock %}
