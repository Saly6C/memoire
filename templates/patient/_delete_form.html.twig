<form method="post" action="{{ path('patient_delete', {'id': patient.id}) }}" onsubmit="return confirm('êtes vous sûr de bien vouloir supprimer?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patient.id) }}">
    <button class="btn btn-primary btn-action my-2">Supprimer</button>
</form>
