{% extends 'base.html.twig' %}

{% block title %}Modifier Personnel{% endblock %}

{% block content %}
    <div class="container py-2">
        <h1>Modifier Personnel</h1>

        {{ include('personnel/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('personnel_index') }}"><i class="fa fa-arrow-left" aria-hidden="true">Back</i></a>

        {{ include('personnel/_delete_form.html.twig') }}
    </div>
{% endblock %}
