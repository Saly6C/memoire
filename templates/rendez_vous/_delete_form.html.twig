<form method="post" action="{{ path('rendez_vous_delete', {'id': rendez_vous.id}) }}" onsubmit="return confirm('Etes-vous sûr de bien vouloir supprimer cet enregistrement?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vous.id) }}">
    <button class="btn btn-primary btn-action my-2">Supprimer</button>
</form>
