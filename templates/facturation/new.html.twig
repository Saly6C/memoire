{% extends 'base.html.twig' %}
{% block title %}Ajouter Facturation{% endblock %}

{% block facturation %}
    {{parent()}}
    <style type="text/css">
        .block_facturation { background-color: #c7c7c7}
    </style>
{% endblock %}

{% block content %}
    
    <div class="container py-2">
            <h4 class="text-center">Créer une nouvelle facturation</h4>

        {{ include('facturation/_form.html.twig') }}

        {# <a href="{{ path('facturation_index') }}"><i class="fa fa-arrow-left" aria-hidden="true">Back</i></a> #}
        <a href="{{ path('facturation_index') }}" class="d-flex pb-2">
            <i class="fa fa-arrow-left" aria-hidden="true"></i>
            <h6 class="ml-2 text-dark">Back</h6>
        </a>
    </div>
{% endblock %}
